import {COMMON} from 'plugins/common';
import {PLUGIN_CLIENT} from 'plugins/dir/test.client';

console.log(COMMON);
console.log(PLUGIN_CLIENT);
const a = window
window.addEventListener('click', () => {})